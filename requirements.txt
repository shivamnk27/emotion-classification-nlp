# Core data science libraries
numpy>=1.21.0
pandas>=1.3.0

# Machine learning libraries
scikit-learn>=1.0.0

# Deep learning frameworks
tensorflow>=2.10.0,<2.16.0
tf-keras>=2.15.0

# Transformers and NLP
transformers>=4.20.0
tokenizers>=0.13.0

# Additional dependencies for transformers
# torch>=1.12.0  # Required by transformers for some tokenizers
datasets>=2.0.0  # Often used with transformers

# Optional but recommended for better performance
# Uncomment if you want GPU support
# tensorflow-gpu>=2.10.0,<2.16.0

# For reproducibility and environment management
# jupyter>=1.0.0  # If running in Jupyter
# ipykernel>=6.0.0  # If running in Jupyter

# System dependencies (usually handled by conda/system package manager)
# These are typically not installed via pip but listed for reference:
# - CUDA toolkit (for GPU support)
# - cuDNN (for GPU support)